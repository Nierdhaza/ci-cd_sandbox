name: Testing
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: get repo code
        uses: actions/checkout@v3
      - name: install deps
        run: npm ci
      - name: Test
        run: npm run test  
